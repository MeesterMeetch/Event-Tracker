/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import type { SlateSummary } from './slateSummary';
import type { SportScanFailure } from './sportScanFailure';
import type { TopPlay } from './topPlay';

export interface TopPlaysResponse {
  /** May be empty. An empty list with a populated summary is the honest answer on an efficient day, not a failure. */
  picks: TopPlay[];
  summary: SlateSummary;
  /** Sports that returned odds and contributed to the pool. */
  sportsScanned: string[];
  /** Sports skipped without spending a credit because their free event listing showed nothing in the window. Reported so a missing sport reads as "no games tonight" rather than as a silent drop. */
  sportsSkipped: string[];
  /** Sports that errored. Reported rather than swallowed, because a thin board caused by three failed scans means something different from a thin board caused by an efficient market. */
  sportsFailed: SportScanFailure[];
  /** When the fan-out ran. These prices go stale in minutes. */
  scannedAt: Date;
  /** Start of the commence-time window actually applied. */
  windowStart: Date;
  /** End of the commence-time window actually applied. */
  windowEnd: Date;
  /** Priced outcomes dropped for falling outside the window. Lets a client tell "no games left today" apart from "nothing worth betting", which are indistinguishable in an empty picks array. */
  edgesOutsideWindow: number;
}
